- name: install and start nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.packaage:
      name: nginx
      state: started
  - name: run nginx
    ansible.builtin.service:
      name: nginx
      state: started